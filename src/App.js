import React, { Component } from "react";
import logo from "./logo.svg";
import "./App.css";

const App = () => {
  const userData = [{ id: 1, name: "Iulian", location: "London" }];

  return (
    <div>
      <h1>CRUD with HOOKS</h1>
    </div>
  );
};

export default App;
